<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:JumpStart.Model"
             x:Class="JumpStart.JobOfferFeedPage">
    <VerticalStackLayout Spacing="20" Padding="10">
        <CollectionView x:Name="JobsCollectionView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10" BackgroundColor="LightGray" Margin="5" 
                                 VerticalOptions="Start" HorizontalOptions="FillAndExpand">

                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnJobTapped" CommandParameter="{Binding}" />
                        </StackLayout.GestureRecognizers>

                        <Label Text="{Binding JobTitle}" FontSize="24" />
                        <Label Text="{Binding JobDescription}" />
                        <Label Text="{Binding SalaryPerMonth}" />
                        <Label Text="{Binding OfferedBy.Name}"/>
                        <Label Text="{Binding OfferedBy.Profile.Address}" />
                        <Label Text="{Binding OfferedBy.Profile.Description}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
